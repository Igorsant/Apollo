import styled from 'styled-components';

export const Title = styled.h2`
  color: var(--title);
  text-align: center;
`;

export const Subtitle = styled.h3`
  color: var(--title);
  text-align: left;
`;
